{
  "name": "rspack-cjs-interop-bug",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Minimal repro: no import pattern works for both ESM and CJS output when external CJS module uses _export() pattern with __esModule but no exports.default",
  "scripts": {
    "build": "rslib build",
    "test:esm": "node --input-type=module -e \"import fn from './dist/index.js'; fn()\"",
    "test:cjs": "node -e \"require('./dist/index.cjs').default()\"",
    "test": "pnpm build && pnpm test:esm && pnpm test:cjs"
  },
  "dependencies": {
    "fake-cjs-lib": "file:./fake-cjs-lib"
  },
  "devDependencies": {
    "@rslib/core": "^0.22.0",
    "typescript": "^6.0.3"
  }
}
